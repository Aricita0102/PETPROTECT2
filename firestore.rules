rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    // ESTO ABRE TODO PARA PRUEBAS (Solo mientras terminas tu tarea)
    match /{document=**} {
      allow read, write: if true;
    }
  }
}